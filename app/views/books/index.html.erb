<h1>Books</h1>
<% @books.each do |book| %><!--renderの処理でrenderの前に@lists = List.all-->

 <p>Title</p>
 <span>
   <%= link_to book.title, book_path(book.id) %><!--"/bokks/#{book.id}"%>-->
 </span>
 <!--<span>< %= book.title %></span> =とbの間のスペースあり-->
 <p>Body</p>
 <span> <%= book.body %></span>

<% end %>

<!--本の名前、内容、Showに飛ぶ、Editに飛ぶ、Destroyボタンの記述-->
<h2>New book</h2>
<% if @book.errors.any? %><!--バリデーション実装でエラー発生-->
  <%= @book.errors.count %>errors prohibited this book from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
<%= form_with model: @book do |f| %>
  <p>Title</p><!--検証では<label for="book_title">Title</label>-->
  <%= f.text_field :title %>

  <p>Body</p><!--検証では<label for="book_title">Body</label>-->
  <%= f.text_area :body %>

  <%= f.submit 'Create Book' %><!--送信ボタンはBodyの下に来ている-->
<% end %>